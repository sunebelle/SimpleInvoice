# Identity server (OAuth2)
AUTH_BASE_URL=https://is-wso2-dev.101digital.io

# Neobank API
API_BASE_URL=https://api-neobank-dev.101digital.io

# OAuth2 client credentials (server-side only — never expose to the browser)
CLIENT_ID=
CLIENT_SECRET=
