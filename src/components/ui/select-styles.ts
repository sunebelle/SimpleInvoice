const selectBaseClassName =
  "rounded-lg border border-input bg-background text-foreground outline-none focus:border-ring focus:ring-1 focus:ring-ring disabled:opacity-50";

export const selectClassName = `${selectBaseClassName} px-3 py-1.5 text-xs`;

export const selectFullClassName = `${selectBaseClassName} w-full px-4 py-2.5 text-sm`;
